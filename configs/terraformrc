# Terraform CLI Configuration
# This file is copied to ~/.terraformrc during setup

# Plugin cache directory
# Stores provider plugins to avoid re-downloading on every 'terraform init'
# This significantly speeds up repeated initializations and reduces bandwidth usage
plugin_cache_dir = "$HOME/.terraform.d/plugin-cache"

# Disable automatic plugin installation prompts (optional)
# disable_checkpoint = true

